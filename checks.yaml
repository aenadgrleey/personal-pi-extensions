# checks.yaml — declarative check definitions
# Each check runs as a shell command. The check extension reads this file first,
# and still falls back to .pi/checks.yaml for compatibility.

checks:
  - name: tsc
    command: bash .pi/run-package-script-if-present.sh tsc
    description: TypeScript type checking

  - name: biome
    command: bash .pi/run-package-script-if-present.sh lint:biome
    description: Biome linter

  - name: eslint
    command: bash .pi/run-package-script-if-present.sh lint:eslint
    description: TypeScript-aware linter (ESLint)
