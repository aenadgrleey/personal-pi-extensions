# Research: @agney/pi-honcho-memory / agneym/pi-honcho-memory

## Summary

I could not complete verified public-source research in this run because the required web access/search tool is not available in this subagent environment. I therefore did not assume a local install or infer architecture/config/hooks from unverified sources.

## Findings

1. **No verified findings recorded** — The package/repository contents, source paths, tool definitions, configuration schema, hook registration, and comparison with `pi-hermes-memory` could not be confirmed without public source access.

## Sources

- Kept: None — no web/source retrieval tool was available.
- Dropped: None — no search results were available to evaluate.

## Gaps

- Need to inspect the public npm package `@agney/pi-honcho-memory`, if published.
- Need to inspect the public GitHub repository likely named `agneym/pi-honcho-memory`, if accessible.
- Need to compare its implementation against the installed/public `pi-hermes-memory` package by reviewing both source trees, especially extension entrypoints, tool registrations, config handling, lifecycle hooks, and storage/indexing behavior.
- Suggested next steps: rerun this research with `web_search`/URL fetch access enabled, or provide a checked-out source tree or package tarball for direct inspection.
